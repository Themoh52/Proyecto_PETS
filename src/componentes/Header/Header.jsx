//Import all the hooks, pages and css used//
import './Header.css'
import NavBar from '../NavBar/NavBar'

//it contain the header of the website.//
const Header = () =>{
    return(
        <header>
            <NavBar/>
        </header>
    )
}
export default Header